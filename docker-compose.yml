version: '3'
services:
  dev-consul:
    image: "consul:latest"
    container_name: dev-consul
    expose:
      - 8300
      - 8600
      - 8500
    ports:
      - "8300:8300"
      - "8600:8600"
      - "8500:8500"
    command: "agent -dev -ui"
    network_mode: "host"